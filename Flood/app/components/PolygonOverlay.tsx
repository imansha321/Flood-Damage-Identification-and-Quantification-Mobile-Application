// Placeholder component removed to avoid additional dependencies. Using server-rendered overlay image.
export interface FloodPolygon { id: string | number; semantic_change?: string; coordinates: { x: number; y: number }[] }
export default function PolygonOverlay() { return null as any }
